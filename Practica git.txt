Comandos Git. actualizado
 
A)    cmdkey /l
B)    cmdkey /delete:git:https://github.com

repositorio:

git init//inicializa.repositorio
Comandos Git.acualizado

debemos verficar nuestra carpeta y poner (git status)

para verificar (  git config user.name )

git--version// (para saber la version)

cmdkey /delete   (eliminar datos)

git init  (iniciar el repositorio)

git config --global user.name    (se crea el usuario de repositorio)


git config user.name//  (verificar el usuario)

 
A)    cmdkey /l
B)    cmdkey /delete:git:https://github.com

repositorio:

git init//inicializa.repositorio


git add . (agregar los archivos para verificar y enviar a el repositorio)

git commit -m"Primera version" (para pasar iltro)

 git push -u origin master(

sirve para pegar  el url

git chttps://github.com/Emilyinem1007/practica_1007.gitlone :